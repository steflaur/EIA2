declare namespace A11_canvas {
    class Blubber extends BubblingObjects {
        constructor(xPosition: number, yPosition: number);
        setPositions(xPosition: number, yPosition: number): void;
        move(): void;
    }
}
